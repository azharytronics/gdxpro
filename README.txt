GuardexProTech merged publish build (V21).

- Plant hero images are clean background-only assets (no printed text/icons).
- Contact form uses FormSubmit with named fields and submit button; first FormSubmit use may require mailbox activation.
- Brand folders are numeric assets/brands/01..09/. Each contains 01-logo.png (active), 02-logo.png and 03-logo.png (reserved). Replace reserved PNGs with real logos using the same filenames.
- Solution images are under assets/solutions/01..07 with stable filenames.
- Homepage and Partners automatically reveal non-empty 02/03 brand slots.
- 404 uses the standard site-footer (shared with every other page) plus assets/site.css for styling.
- All visual assets are local for offline browsing.
- Tyco is stored in assets/brands/07/01-logo.png.
